package `in`.yashasvi.memorygame.controller


class ScoreManager {

    fun getScore(level: Int, timeToSolve: Int, numOfPairsSolved: Int): Int {
        // logic for calculating score
        // dummy return for now
        return 5;
    }
}